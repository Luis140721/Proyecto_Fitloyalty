{
  "name": "fitloyalty",
  "version": "1.0.0",
  "description": "FitLoyalty CRM - Gimnasios de barrio",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --names \"API,WEB\" --prefix-colors \"yellow,cyan\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
